spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shankar
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect
server:
  port: 9091
